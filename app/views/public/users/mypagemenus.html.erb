<div class="container px-5 px-sm-0">
  <div class="row">
    <% #サイドバー %>
    <div class="col-md-3 mt-4">
       <% #投稿フォーム%>
      <%= render 'public/posts/form', post: @post1 %>
    </div>

    <div class="col-md-8 offset-md-1">
      <% #myprofofile%>
      <%= render 'public/users/myprofile', user: @user %>
      <% #menu一覧 %>
      <% if @menus.count == 0 %>
        メニューが存在しません。<br>
        メニューを作成しましょう！
      <% end %>
      <%= render 'public/users/mymenu', menus: @menus %>
    </div>
  </div>
</div>